import { styled } from 'stitches.config';

export const Wrapper = styled('button', {
  width: 300,
  height: 40,
  borderRadius: '$full',

  color: '$text',
  background: '$primary',

  fontWeight: '$medium',
});
